{
  "name": "m365-renew-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .build/ .serverless/ dist/",
    "build": "tsc",
    "predeploy": "npm run clean && npm run prettier && npm run lint && npm run build",
    "deploy": "serverless deploy",
    "prettier": "prettier --write .",
    "lint": "eslint --ignore-pattern dist/ --ignore-pattern .serverless/ --ext .ts,.tsx --fix .",
    "tsc": "tsc --noemit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1010.x",
    "@aws-sdk/client-sns": "^3.1012.x",
    "@aws-sdk/lib-dynamodb": "^3.1027.x",
    "@sparticuz/chromium": "^147.0.x",
    "otplib": "^13.4.x",
    "puppeteer-core": "^24.40.x",
    "swagger-ui-dist": "^5.32.x",
    "uuid": "^13.0.x"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.x",
    "@types/node": "^25.5.x",
    "eslint": "^10.2.x",
    "eslint-config-prettier": "^10.1.x",
    "eslint-plugin-prettier": "^5.5.x",
    "globals": "^17.4.x",
    "prettier": "^3.8.x",
    "serverless": "^4.34.x",
    "typescript": "^6.0.x",
    "typescript-eslint": "^8.57.x"
  }
}
